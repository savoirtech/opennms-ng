<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <parent>
        <artifactId>bundles</artifactId>
        <groupId>org.opennms.ng</groupId>
        <version>1.0-SNAPSHOT</version>
    </parent>
    <modelVersion>4.0.0</modelVersion>

    <name>OPENNMS :: C3P0</name>
    <description>OPENNMS C3P0</description>

    <groupId>org.opennms.bundles</groupId>
    <artifactId>c3p0</artifactId>
    <version>${c3p0.version}</version>

    <packaging>bundle</packaging>

    <properties>
        <pkgGroupId>org.opennms.bundles</pkgGroupId>
        <pkgArtifactId>c3p0</pkgArtifactId>
        <pkgVersion>${c3p0.version}</pkgVersion>

        <opennms.osgi.import.pkg>
            !com.mchange*,
            *
        </opennms.osgi.import.pkg>
        <opennms.osgi.export>
            com.mchange*;version="${c3p0.version}"
        </opennms.osgi.export>

        <opennms.osgi.include.resource>{maven-resources},{maven-dependencies}</opennms.osgi.include.resource>
        <opennms.osgi.failok>true</opennms.osgi.failok>

    </properties>

    <dependencies>

        <dependency>
            <groupId>c3p0</groupId>
            <artifactId>c3p0</artifactId>
            <version>${c3p0.version}</version>
        </dependency>

    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-shade-plugin</artifactId>
                <executions>
                    <execution>
                        <phase>package</phase>
                        <goals>
                            <goal>shade</goal>
                        </goals>
                        <configuration>
                            <artifactSet>
                                <includes>
                                    <include>${pkgGroupId}:${pkgArtifactId}</include>
                                </includes>
                            </artifactSet>

                            <promoteTransitiveDependencies>true</promoteTransitiveDependencies>
                            <createDependencyReducedPom>true</createDependencyReducedPom>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>

</project>
